{
  "name": "hexagons",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url /hexagons/",
    "deploy": "rimraf dist && npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "gh-pages": "^2.2.0",
    "three": "^0.112.1",
    "three-orbit-controls": "^82.1.0"
  }
}
